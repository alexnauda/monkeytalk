#!/bin/bash

# Build config for the build script, build.sh. Look there for more info.

APP_NAME=monkeytalk-recorder
CHROME_PROVIDERS="content skin"
CLEAN_UP=1
ROOT_FILES="readme.txt"
ROOT_DIRS="components"
BEFORE_BUILD=
AFTER_BUILD=
